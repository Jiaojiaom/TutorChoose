+ 我使用的是xampp下的tomcat的服务器
+ 数据库也是xampp下的mysql

+ 导入数据库

```

d:

# 进入到mysql的bin目录下

cd xampp/mysql/bin

# 导入创建数据库文件

source E:\sql\DataCreate(mysql).sql

```

excel表格必须是Microsoft Excel 97-2003工作簿格式，否则会报错